import { StaffDetailsViewModel } from './staff-details-view-model';

describe('StaffDetailsViewModel', () => {
  it('should create an instance', () => {
    expect(new StaffDetailsViewModel()).toBeTruthy();
  });
});
