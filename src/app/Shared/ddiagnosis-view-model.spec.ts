import { DDiagnosisViewModel } from './ddiagnosis-view-model';

describe('DDiagnosisViewModel', () => {
  it('should create an instance', () => {
    expect(new DDiagnosisViewModel()).toBeTruthy();
  });
});
