import { DAppointmentViewModel } from './dappointment-view-model';

describe('DAppointmentViewModel', () => {
  it('should create an instance', () => {
    expect(new DAppointmentViewModel()).toBeTruthy();
  });
});
