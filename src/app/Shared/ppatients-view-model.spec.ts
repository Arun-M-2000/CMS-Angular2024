import { PPatientsViewModel } from './ppatients-view-model';

describe('PPatientsViewModel', () => {
  it('should create an instance', () => {
    expect(new PPatientsViewModel()).toBeTruthy();
  });
});
