import { LPrescriptionViewModel } from './lprescription-view-model';

describe('LPrescriptionViewModel', () => {
  it('should create an instance', () => {
    expect(new LPrescriptionViewModel()).toBeTruthy();
  });
});
