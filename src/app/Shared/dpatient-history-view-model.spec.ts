import { DPatientHistoryViewModel } from './dpatient-history-view-model';

describe('DPatientHistoryViewModel', () => {
  it('should create an instance', () => {
    expect(new DPatientHistoryViewModel()).toBeTruthy();
  });
});
